import { CodeFolder, TaskForCode } from '@automa/bot';

export const update = async (folder: CodeFolder, task: TaskForCode) => {
  // Update code in the specified folder
  //
  // If adding a new file, use the following:
  // await folder.add('example.ts');
};
